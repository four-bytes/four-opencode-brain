import type { Plugin } from "@opencode-ai/plugin";

export default (async ({ client, project, directory, $ }) => {
  // Wave P12a — foundation only; tools registered in later waves

  return {};
}) satisfies Plugin;
